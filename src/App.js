import React, {useEffect} from 'react';
import {View, Text} from 'react-native';
import {Splash, Login} from './pages';

const App = () => {
  // return <Splash />;
  return <Login />;
};

export default App;
